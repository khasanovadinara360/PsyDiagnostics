<UserControl x:Class="PsyDiagnostics.Views.ResultView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:PsyDiagnostics.Helpers">

    <Grid>
        <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тест" Binding="{Binding TestName}" />
                <DataGridTextColumn Header="Баллы" Binding="{Binding Score}" />
                <DataGridTextColumn Header="Дата" Binding="{Binding Date}" />
                <DataGridTextColumn Header="Прогноз">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Prediction">
                            <Binding.Converter>
                                <local:PredictionConverter/>
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>