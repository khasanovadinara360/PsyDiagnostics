<UserControl x:Class="PsyDiagnostics.Views.RoleSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#1E1E28">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="420">

            <TextBlock Text="Выберите роль"
                       Foreground="White"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <Button Content="Психолог"
                    Command="{Binding SelectPsychologistRoleCommand}"
                    Height="50"
                    Margin="0,0,0,15"
                    Background="#4E5DFF"
                    Foreground="White"/>

            <Button Content="Заключённый"
                    Command="{Binding SelectPrisonerRoleCommand}"
                    Height="50"
                    Background="#252532"
                    Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>