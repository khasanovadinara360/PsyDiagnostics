<UserControl x:Class="PsyDiagnostics.Views.TestSelectionView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#1E1E2F">

        <StackPanel Margin="30">

            <!-- Заголовок -->
            <TextBlock Text="Выбор теста"
                       FontSize="26"
                       Foreground="White"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <!-- 🔥 СПИСОК ТЕСТОВ -->
            <ListBox ItemsSource="{Binding Tests}"
                     Background="Transparent"
                     BorderThickness="0">

                <!-- 🔥 КЛЮЧЕВОЙ ФИКС -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <!-- Вертикальный список -->
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <!-- Шаблон элемента -->
                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <!-- 🔥 КНОПКА (ГЛАВНОЕ) -->
                        <Button Content="{Binding DisplayName}"
                                Margin="0,5"
                                Padding="12"
                                FontSize="16"
                                Background="#2E2E4D"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"

                                Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}">

                            <!-- Красивый hover -->
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#2E2E4D"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#3E3E6D"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>

                        </Button>

                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

        </StackPanel>

    </Grid>
</UserControl>