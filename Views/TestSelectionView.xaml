<UserControl x:Class="PsyDiagnostics.Views.TestSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2C2C40"/>
            <Setter Property="BorderBrush" Value="#4A4A6A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A60"/>
                    <Setter Property="BorderBrush" Value="#6A6AC0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#1E1E2F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Выберите тесты"
                   Margin="0,10"
                   Foreground="White"
                   FontSize="18"
                   HorizontalAlignment="Center"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Tests}"
                 Margin="20"
                 Background="Transparent"
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsSelected}"
                                  Margin="0,0,10,0"/>

                        <TextBlock Text="{Binding DisplayName}"
                                   Foreground="White"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10">

            <Button Content="Назад"
                    Command="{Binding BackCommand}"
                    Style="{StaticResource ActionButtonStyle}"/>

            <Button Content="Добавить тест"
                    Command="{Binding DataContext.AddTestCommand,
                                      RelativeSource={RelativeSource AncestorType=Window}}"
                    Visibility="{Binding DataContext.PsychologistVisibility,
                                         RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource ActionButtonStyle}"/>

            <Button Content="Начать"
                    Command="{Binding StartCommand}"
                    Margin="0"
                    Style="{StaticResource ActionButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>